<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.community">

	<select id="selectAll" resultType="community">
		select comm_no,comm_cate_nm, comm_date, comm_title, comm_content, comm_hitno, mid
		from community 
		order by comm_date desc 
	</select>
 
 	<select id="selectChat" resultType="community">
		select comm_no,comm_cate_nm, comm_date, comm_title, comm_content, comm_hitno, mid ,comm_cate_nm, comm_cate_no
		from community 
		where comm_cate_no = '1'
		order by comm_date desc 
	</select>
 	
 	<select id="selectTips" resultType="community">
		select comm_no,comm_cate_nm, comm_date, comm_title, comm_content, comm_hitno, mid ,comm_cate_nm, comm_cate_no
		from community 
		where comm_cate_no = '2'
		order by comm_date desc 
	</select>
 
	<select id="selectBoast" resultType="community">
		select comm_no,comm_cate_nm, comm_date, comm_title, comm_content, comm_hitno, mid ,comm_cate_nm, comm_cate_no
		from community 
		where comm_cate_no = '3'
		order by comm_date desc 
	</select> 
	
	<select id="selectAssign" resultType="community">
		select comm_no,comm_cate_nm, comm_date, comm_title, comm_content, comm_hitno, mid ,comm_cate_nm, comm_cate_no
		from community 
		where comm_cate_no = '4'
		order by comm_date desc 
	</select>
 
	<select id="selectReview" resultType="review">
		select review_no, review_date, review_title, review_content, review_hitno, mid, class_no, review_star 
		from review 
		order by review_date desc  
	</select>
	
	
	
	
</mapper>