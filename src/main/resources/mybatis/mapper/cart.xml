<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.cart">

	<!-- <insert id="insert" parameterType="cart">
		insert into CART(member_id, class_no, class_nm, class_thum, class_price)
		values(#{member_id}, #{class_no}, #{class_nm}, #{class_thum}, #{class_price})
	</insert> -->
	<!-- cart테이블 구입 목록저장 -->
	<insert id="insert1" parameterType="cart">
		insert into CART (Mid, class_no)
		values (#{mid}, #{class_no})
		
	</insert>
	
	<!-- 마이페이지 수강내역 목록저장 -->
	<insert id="insert2" parameterType="cart" >
		insert into class_Appl (Mid, class_no, cart_date)
		values (#{mid}, #{class_no}, #{cart_date}) form cart
		
	</insert>
	
	
	<select id="selectbyClassNo" resultType="cart">
		select class_no, class_nm_s, class_nm, class_thum, class_price from class where class_no=#{class_no}
	</select>
	<!-- <select id="selectAll" resultType="cart">
		 select m.member_id, cs.class_no, class_nm, class_thum, class_price
		 from MEMBER m, CART c, CLASS cs
		 where m.member_id = c.member_id and c.class_no=cs.class_no;
	</select>
	
	<select id="selectbyClassNo" resultType="class" parameterType="int">
		select * from class where class_no = #{class_no}
	</select>
 -->
	
	
</mapper>