<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.cart">

	<insert id="insert" parameterType="cart">
		insert into CART(user_id, class_no, class_nm, class_thum, class_price)
		values(#{user_id}, #{class_no}, #{class_nm}, #{class_thum}, #{class_price})
	</insert>
	
	<select id="selectAll" resultType="cart">
		 select u.user_id, cs.class_no, class_nm, class_thum, class_price
		 from USER u, CART c, CLASS cs
		 where u.user_id = c.user_id and c.class_no=cs.class_no;
	</select>
	
	<select id="countAll" resultType="cart">
		select count(*) from CART
	</select>
	
</mapper>